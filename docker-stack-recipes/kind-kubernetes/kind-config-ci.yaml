# Slimmer cluster for CI (1 control-plane + 1 worker): faster and less brittle than
# full 1+2 on GitHub-hosted runners. Local developers should prefer kind-config.yaml.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
